// src/lib/better-auth-client.ts
// Placeholder file to avoid import errors
// We're using our custom API service instead of the Better Auth client

// Define placeholder functions that will never be called
export const signUp = async () => {
  throw new Error("Better Auth client is not used. Use authService instead.");
};

export const signIn = async () => {
  throw new Error("Better Auth client is not used. Use authService instead.");
};

export const signOut = async () => {
  throw new Error("Better Auth client is not used. Use authService instead.");
};

export const getSession = async () => {
  throw new Error("Better Auth client is not used. Use authService instead.");
};